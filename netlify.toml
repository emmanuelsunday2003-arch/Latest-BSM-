[build]
  command = "npm run build"
  publish = "dist"

# Rule to allow SPA routing (React Router) to work correctly when reloading pages
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
